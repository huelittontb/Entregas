name: Build APK (Kivy + Buildozer)

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: 🧱 Checkout código
        uses: actions/checkout@v4

      - name: ⚡ Cache Buildozer & Gradle
        uses: actions/cache@v4
        with:
          path: |
            .buildozer
            ~/.buildozer
            ~/.gradle
          key: buildozer-${{ runner.os }}-${{ hashFiles('**/buildozer.spec') }}
          restore-keys: |
            buildozer-${{ runner.os }}-

      - name: 🐳 Build APK no container Buildozer
        uses: addnab/docker-run-action@v3
        with:
          image: kivy/buildozer:latest
          options: -v ${{ github.workspace }}:/home/user/app -w /home/user/app --user root
          run: |
            echo "⚙️ Limpando build anterior..."
            rm -rf .buildozer bin || true

            echo "⬇️ Instalando dependências..."
            pip install --upgrade pip buildozer

            echo "🩹 Aplicando patch para ignorar check_root()..."
            sed -i 's/def check_root(self):/def check_root(self):\n        return/' $(python3 -c "import buildozer; import inspect; print(inspect.getfile(buildozer))") || true

            echo "🚀 Compilando APK..."
            buildozer -v android debug

            echo "📦 Renomeando APK..."
            mkdir -p bin || true
            APK_PATH=$(find . -name "*.apk" | head -n 1)
            if [ -f "$APK_PATH" ]; then
              cp "$APK_PATH" bin/app-release.apk
              echo "✅ APK gerado em: bin/app-release.apk"
            else
              echo "❌ Nenhum APK encontrado!"
              exit 1
            fi

      - name: 📤 Upload do APK como artefato
        uses: actions/upload-artifact@v4
        with:
          name: app-release
          path: bin/app-release.apk

      - name: 🏷️ Publicar Release automática
        if: github.ref == 'refs/heads/main'
        uses: softprops/action-gh-release@v2
        with:
          tag_name: "build-${{ github.run_number }}"
          name: "Build #${{ github.run_number }}"
          files: bin/app-release.apk
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
