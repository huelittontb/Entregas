name: Build APK (Kivy + Buildozer)

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: 🧱 Checkout código
        uses: actions/checkout@v4

      - name: 🐳 Build APK com Buildozer
        uses: addnab/docker-run-action@v3
        with:
          image: kivy/buildozer:latest
          options: -v ${{ github.workspace }}:/home/user/app -w /home/user/app --user root
          run: |
            echo "⚙️ Limpando build anterior..."
            rm -rf .buildozer bin || true

            echo "⬇️ Instalando dependências..."
            pip install --upgrade pip buildozer

            echo "🩹 Ignorando check_root()..."
            BUILD_PATH=$(python3 -c "import buildozer, os; print(os.path.dirname(buildozer.__file__))")
            sed -i '/def check_root(self):/a\        return' "$BUILD_PATH/__init__.py"

            echo "📦 Configurando SDK e variáveis..."
            export ANDROID_SDK_ROOT=/root/.buildozer/android/platform/android-sdk
            export PATH=$ANDROID_SDK_ROOT/cmdline-tools/latest/bin:$ANDROID_SDK_ROOT/platform-tools:$PATH

            echo "⬇️ Instalando dependências do sistema..."
            apt-get update -qq
            apt-get install -y wget unzip openjdk-17-jdk

            echo "📥 Instalando Android Command Line Tools..."
            mkdir -p $ANDROID_SDK_ROOT/cmdline-tools
            cd $ANDROID_SDK_ROOT/cmdline-tools
            wget -q https://dl.google.com/android/repository/commandlinetools-linux-11076708_latest.zip -O tools.zip
            unzip -q tools.zip
            mv cmdline-tools latest
            rm tools.zip
            cd /home/user/app

            echo "✅ Aceitando todas as licenças..."
            yes | sdkmanager --sdk_root=$ANDROID_SDK_ROOT --licenses || true

            echo "⬇️ Instalando componentes do SDK..."
            yes | sdkmanager --sdk_root=$ANDROID_SDK_ROOT \
              "platform-tools" \
              "platforms;android-35" \
              "build-tools;35.0.0" \
              "ndk;25.1.8937393" \
              "cmdline-tools;latest"

            echo "🚀 Compilando APK..."
            buildozer -v android debug || (echo "❌ Falha na compilação"; exit 1)

            echo "📦 Procurando APK gerado..."
            mkdir -p bin || true
            APK_PATH=$(find . -name "*.apk" | head -n 1)
            if [ -f "$APK_PATH" ]; then
              cp "$APK_PATH" bin/app-release.apk
              echo "✅ APK gerado com sucesso em: bin/app-release.apk"
            else
              echo "❌ Nenhum APK encontrado!"
              exit 1
            fi

      - name: 📤 Upload do APK como artefato
        uses: actions/upload-artifact@v4
        with:
          name: app-release
          path: bin/app-release.apk
